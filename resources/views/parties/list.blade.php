<div class="page-title"><div><h1>{{$title}}</h1><p>{{ __('pos.manage_contacts') }}</p></div><button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#add">+ {{ __('pos.add') }}</button></div><form class="mb-3"><input class="form-control" name="search" value="{{request('search')}}" placeholder="{{ __('pos.search_name_phone') }}"></form><div class="card p-3 table-responsive"><table class="table"><thead><tr><th>{{ __('pos.code') }}</th><th>{{ __('pos.name') }}</th><th>{{ __('pos.phone') }}</th><th>{{ __('pos.address') }}</th><th>{{ __('pos.balance') }}</th></tr></thead><tbody>@foreach($list as $x)<tr><td>{{$x->code}}</td><td><b>{{$x->name}}</b><small class="d-block">{{$x->type??$x->contact_person}}</small></td><td>{{$x->phone}}</td><td>{{$x->address}}</td><td class="text-danger">{{ \App\Support\Currency::format($x->outstanding_balance) }}</td></tr>@endforeach</tbody></table>{{$list->links()}}</div><div class="modal fade" id="add"><div class="modal-dialog"><form method="post" action="{{route($route)}}" class="modal-content">@csrf<div class="modal-header"><h5>{{ __('pos.add') }} {{ $title }}</h5></div><div class="modal-body"><input name="name" class="form-control mb-2" placeholder="{{ __('pos.name') }}" required><input name="phone" class="form-control mb-2" placeholder="{{ __('pos.phone') }}"><textarea name="address" class="form-control mb-2" placeholder="{{ __('pos.address') }}"></textarea>@if($types)<select name="type" class="form-select">@foreach($types as $t)<option>{{$t}}</option>@endforeach</select>@else<input name="contact_person" class="form-control mb-2" placeholder="{{ __('pos.contact_person') }}">@endif<textarea name="notes" class="form-control mt-2" placeholder="{{ __('pos.notes') }}"></textarea></div><div class="modal-footer"><button class="btn btn-primary">{{ __('pos.save') }}</button></div></form></div></div>
